Build a manager-ready status report from your GitHub or git work in the last two weeks.

Determine your git identity from `git config user.name` and/or `git config user.email`, unless the TODO text explicitly provides an author.
Collect commits authored by you from the last 14 days.
If the current directory is not a git repo, scan each immediate child git repo.

Synthesize a high-level report that groups related work into themes, calls out notable outcomes and impact, and includes concise next steps or open risks.
Keep the language non-technical enough for manager-level comprehension.

Append the report to the active `todo.md` file unless explicitly asked for another destination.
