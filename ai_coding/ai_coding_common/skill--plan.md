Create a step-by-step plan that Codex or Claude Code can follow to implement the text following the `$PLAN` skill.

Before writing the plan, research the existing codebase for reusable methods and patterns.
Small backward-compatible modifications are allowed.
Look to reuse as much existing code as possible.

The plan must be accurate and detailed enough for an LLM to execute autonomously in a vacuum.
Optimize for LLM comprehension and execution.
Take the time needed to produce the best plan.
